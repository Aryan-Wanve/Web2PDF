{
  "name": "web2pdf",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension that converts rendered document viewer pages into a local PDF.",
  "scripts": {
    "validate": "node scripts/validate-extension.js",
    "package": "powershell -ExecutionPolicy Bypass -File scripts/package-extension.ps1",
    "build": "npm run validate && npm run package"
  }
}
