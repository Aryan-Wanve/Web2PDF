# Vendored Libraries

- `jspdf.umd.min.js`: jsPDF 2.5.2, bundled from the official npm package so the Manifest V3 extension can generate PDFs without loading remote code.

jsPDF is distributed under the MIT license by the jsPDF project.
